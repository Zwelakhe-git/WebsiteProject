// Импорты не нужны при использовании глобальных переменных
// из CDN, но для ясности можно добавить комментарии

// React и ReactDOM доступны глобально через script-теги
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);